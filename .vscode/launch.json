{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Launch file in Chrome",
            "url": "${file}",
            "webRoot": "${workspaceRoot}"
        }
    ]
}